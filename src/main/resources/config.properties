# Application Configuration
app.url=http://auslan-ds-canary-app.s3-website-ap-southeast-2.amazonaws.com/#/authenticate/logout

# Browser Configuration
browser=chromium
headless=false
# Note: headless=false means browser will run in HEAD mode (visible)

# Test User Credentials
test.email=sarvesh@curvetomorrow.com.au
test.password=Curve@2025

# Test Data
client.name=Sarvesh newndis
address=2 Victory Road, Clarinda VIC, Australia
phone.number=0488883690

# Timeouts (in milliseconds)
timeout.short=1000
timeout.medium=2000
timeout.long=5000

# Email Configuration for Test Reports
email.to=sarvesh@curvetomorrow.com.au
email.from=auslan-automation@curvetomorrow.com.au

# SMTP Configuration (Gmail)
# To use Gmail, generate an App Password:
# 1. Go to Google Account → Security → 2-Step Verification
# 2. Scroll down to App passwords
# 3. Generate a new app password for "Mail"
# 4. Use that password below (not your regular Gmail password)
smtp.host=smtp.gmail.com
smtp.port=587
smtp.username=
smtp.password=

# Note: Leave smtp.username and smtp.password empty to disable email notifications
# Fill them with valid credentials to enable automatic email reports
